vich_uploader:
    db_driver: orm

    mappings:
        ads: # C'est ce nom "ads" que le message d'erreur ne trouvait pas
            uri_prefix: /images/ads
            upload_destination: '%kernel.project_dir%/public/images/ads'
            namer: Vich\UploaderBundle\Naming\UniqidNamer